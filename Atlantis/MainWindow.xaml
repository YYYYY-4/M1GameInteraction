<Window x:Class="Atlantis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Atlantis"
        xmlns:a="clr-namespace:Atlantis"
        xmlns:game="clr-namespace:Atlantis.Game"
        xmlns:scene="clr-namespace:Atlantis.Scene"
        xmlns:menu="clr-namespace:Atlantis.Menus"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="800" 
        Width="1200"
        Background="#295D8A">
    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="1" FontSize="72" HorizontalAlignment="Center" FontFamily="Old English Text MT">Atlantis</TextBlock>
            <Button Grid.Row="1" Grid.Column="0" Click="Start_Button_Click">Start</Button>
            <Button Grid.Row="3" Grid.Column="0" Click="Quit_Button_Click">Quit</Button>
        </Grid>
</Window>
